# Direct dependencies (all components). For an exact pinned install use
# `uv sync` (uv.lock). The deployed backend installs only the [project]
# dependencies below — see pyproject.toml groups.

# --- backend runtime ---
fastapi>=0.141.1
uvicorn>=0.52.4
langgraph>=1.2.11
langgraph-checkpoint-postgres>=3.1.2
langchain-anthropic>=1.7.1
langchain-core>=1.6.2
langchain-neo4j>=0.10.0
langchain-text-splitters>=1.1.2
pydantic>=2.13.5
pinecone>=10.0.0
voyageai>=0.5.0
tavily-python>=0.8.1
psycopg[binary]>=3.3.5
psycopg-pool>=3.3.1
python-dotenv>=1.2.3
pyjwt>=2.10.1

# --- frontend (Next.js UI) --- see frontend/package.json, not a Python dep

# --- data prep (build scripts only) ---
pandas>=3.0.5
langchain-experimental>=0.4.2
